<%= render "shared/header" %>
<div class="container">
  <div class="row">
    <%= render "shared/left-nav" %>
    <div class="col-9 pt-3">
      <%= render @games %>
      <%= paginate @games %>
    </div>
  </div>
</div>
